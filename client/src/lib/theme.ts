"use client";
import { createTheme } from "@mui/material/styles";

export const robotoFonts = "var(--font-roboto)";

const theme = createTheme({
  typography: {
    fontFamily: "var(--font-noto-sans)",
  },
});

export default theme;
